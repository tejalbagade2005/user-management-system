server.port=8080

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/fullstack_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}
spring.jpa.properties.hibernate.dialect=${DB_DIALECT:org.hibernate.dialect.MySQL8Dialect}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
spring.datasource.hikari.initialization-fail-timeout=0
spring.sql.init.mode=never

jwt.secret=${JWT_SECRET:fullstack-jwt-secret-key-change-me}
jwt.expiration=${JWT_EXPIRATION:86400000}

spring.mvc.pathmatch.matching-strategy=ant_path_matcher

logging.level.com.example.backend=DEBUG
logging.level.org.springframework.security=DEBUG
